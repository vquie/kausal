# Development-only service account manifest for the local Kausal deployment.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kausal
  namespace: kausal
  labels:
    app.kubernetes.io/name: kausal
    app.kubernetes.io/part-of: kausal
    app.kubernetes.io/component: backend
    app.kubernetes.io/environment: dev
